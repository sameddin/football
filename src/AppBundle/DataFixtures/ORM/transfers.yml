AppBundle\Entity\Transfer:
    transfer{1..100}:
        date: <date($format = 'Y-m-d', $max = 'now')>
        sum: <randomFloat($nbMaxDecimals = NULL, $min = 0, $max = 200)>
        player: @player*
        team: @team*
