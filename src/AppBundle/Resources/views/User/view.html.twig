{% extends 'layout.html.twig' %}

{% block content %}
    <div>
        <h3 class="page">{{ user.firstName }} {{ user.lastName }}</h3>

        <div class="row">
            <div class="col-xs-4">
                <img class="img-responsive" src="{{ gravatar(user.email, 200) }}"/>
            </div>

        </div>
    </div>
{% endblock %}
