{% extends 'layout.html.twig' %}

{% block content %}

<h3>{{ tournament.name }}</h3>

<ul class="nav nav-tabs">
    <li role="presentation"><a href="{{ path('tournament.news', {id: tournament.id }) }}">{{ 'newsline'|trans }}</a></li>
    <li role="presentation"><a href="{{ path('tournament.table', {id: tournament.id }) }}">{{ 'table'|trans }}</a></li>
    <li role="presentation" class="active"><a href="{{ path('tournament.calendar', {id: tournament.id }) }}">{{ 'calendar'|trans }}</a></li>
    <li role="presentation"><a href="{{ path('tournament.stat', {id: tournament.id }) }}">{{ 'stat'|trans }}</a></li>
</ul>

    <table class="table table-condensed">
        <thead>
        <tr>
            <th>Дата</th>
            <th>Хозяева</th>
            <th>Счёт</th>
            <th>Гости</th>
        </tr>
        </thead>
        <tr>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    </table>

{% endblock %}
