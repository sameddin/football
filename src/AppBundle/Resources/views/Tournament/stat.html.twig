{% extends 'base.html.twig' %}

{% block body %}

    <h3>{{ tournament.name }}</h3>

    <ul class="nav nav-tabs">
        <li role="presentation"><a href="{{ path('tournament.news', {id: tournament.id }) }}">{{ 'newsline'|trans }}</a></li>
        <li role="presentation"><a href="{{ path('tournament.table', {id: tournament.id }) }}">{{ 'table'|trans }}</a></li>
        <li role="presentation"><a href="{{ path('tournament.calendar', {id: tournament.id }) }}">{{ 'calendar'|trans }}</a></li>
        <li role="presentation" class="active"><a href="{{ path('tournament.stat', {id: tournament.id }) }}">{{ 'stat'|trans }}</a></li>
    </ul>

    <table class="table table-condensed">
        <thead>
        <tr>
            <th>Место</th>
            <th>Игрок</th>
            <th>Команда</th>
            <th>Матчи</th>
            <th>Голы</th>
            <th>Пенальти</th>
            <th>Ассисты</th>
            <th>Гол + Пас</th>
        </tr>
        </thead>
        <tr>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    </table>

{% endblock %}
