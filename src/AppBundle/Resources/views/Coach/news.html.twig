{% extends 'base.html.twig' %}

{% block body %}

    <h3>{{ coach.name }}</h3>

    <ul class="nav nav-tabs">
        <li role="presentation" class="active"><a href="{{ path('coach.news', {id: coach.id }) }}">{{ 'newsline'|trans }}</a></li>
    </ul>

{% endblock %}
